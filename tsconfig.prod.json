{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "exclude": [
    // Exclude test files and node_modules for production build
    "**/*.test.ts",
    "**/*.spec.ts",
    "src/specs",
    "node_modules"
  ]
}
